import { StyleSheet } from "react-native";
import { defaultStyle } from "../../styles/variable";

const styles = StyleSheet.create({
	advertWrapper: {
		borderRadius: 8,
		backgroundColor: "#dee4f7",
		color: defaultStyle.baseTextColor,
		padding: 8,
        textAlign:'left',
		borderColor: "#2C62EE",
		borderWidth: 1,
		marginTop: 16,
        marginHorizontal:14, 
        height:60,
        flex:1,
        
        
	},
});

export default styles;
